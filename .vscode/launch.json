{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug (CodeLLDB)",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/build/pbr",
            "args": [],
            "cwd": "${workspaceFolder}/build",
            "preLaunchTask": "CMake Build"
        }
    ]
}